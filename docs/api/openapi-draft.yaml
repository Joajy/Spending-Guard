openapi: 3.1.0
info:
  title: SpendGuard API
  version: 0.2.0-draft
  description: 현재 구현과 후속 기능의 API 계약 초안
servers:
  - url: http://localhost:8080
tags:
  - name: Users
  - name: Spend Events
  - name: Transactions
  - name: Budgets
  - name: Dashboard
  - name: Integrations
paths:
  /api/v1/users:
    post:
      tags: [Users]
      summary: Register a user account
      operationId: registerUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterUserRequest'
      responses:
        '201':
          description: User account created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRegisteredResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Email already registered
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /api/v1/spend-events:
    post:
      tags: [Spend Events]
      summary: Submit a financial notification text
      operationId: submitSpendEvent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitSpendEventRequest'
      responses:
        '202':
          description: Event accepted for asynchronous processing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpendEventAcceptedResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Duplicate event
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /api/v1/spend-events/{eventId}:
    get:
      tags: [Spend Events]
      summary: Get processing status and result
      operationId: getSpendEvent
      parameters:

    UserId:
      name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    OrderId:
      name: orderId
      in: path
      required: true
      schema:
        type: string
        minLength: 6
        maxLength: 64
        - $ref: '#/components/parameters/EventId'
      responses:
        '200':
          description: Current event state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpendEventDetail'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/transactions:
    get:
      tags: [Transactions]
      summary: List normalized transactions
      operationId: listTransactions
      parameters:
        - name: month
          in: query
          required: false
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
        - name: category
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Category'
      responses:
        '200':
          description: Transaction page
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Transaction'
  /api/v1/transactions/{transactionId}/category:
    patch:
      tags: [Transactions]
      summary: Correct a transaction category
      operationId: correctCategory
      parameters:
        - name: transactionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category]
              properties:
                category:
                  $ref: '#/components/schemas/Category'
      responses:
        '200':
          description: Corrected transaction
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
  /api/v1/budgets/{month}:
    get:
      tags: [Budgets]
      summary: Get monthly budgets
      operationId: getMonthlyBudgets
      parameters:
        - $ref: '#/components/parameters/Month'
      responses:
        '200':
          description: Monthly budget status
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Budget'
    put:
      tags: [Budgets]
      summary: Replace monthly category budgets
      operationId: replaceMonthlyBudgets
      parameters:
        - $ref: '#/components/parameters/Month'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              minItems: 1
              items:
                $ref: '#/components/schemas/BudgetInput'
      responses:
        '200':
          description: Saved budgets
  /api/v1/dashboard/{month}:
    get:
      tags: [Dashboard]
      summary: Get monthly dashboard projection
      operationId: getDashboard
      parameters:
        - $ref: '#/components/parameters/Month'
      responses:
        '200':
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Dashboard'
  /api/v1/integrations/toss-payments/webhook:
    post:
      tags: [Integrations]
      summary: Receive and verify a Toss Payments status webhook
      description: Resolves paymentKey through the Toss Payments API. It represents the connected merchant, not the user's complete card history.
      operationId: receiveTossPaymentsWebhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TossPaymentWebhookRequest'
      responses:
        '200':
          description: Verified event accepted or acknowledged as a duplicate
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TossPaymentWebhookResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          description: Payment verification or integration configuration failed
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Problem'
  /api/v1/users/{userId}/integrations/toss-payments/test-orders:
    post:
      tags: [Integrations]
      summary: Create a server-bound Toss Payments test order
      operationId: createTossTestOrder
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTossTestOrderRequest'
      responses:
        '200':
          description: Test order and public client key
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TossTestOrder'
        '400':
          $ref: '#/components/responses/BadRequest'
        '503':
          description: Toss Payments integration is not configured
  /api/v1/users/{userId}/integrations/toss-payments/test-orders/{orderId}/confirm:
    post:
      tags: [Integrations]
      summary: Confirm and ingest a Toss Payments test order
      operationId: confirmTossTestOrder
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmTossTestOrderRequest'
      responses:
        '200':
          description: Confirmed payment ingestion result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TossTestOrderResult'
        '409':
          description: Order has already been processed or does not match
        '503':
          description: Toss Payments confirmation failed
  /api/v1/users/{userId}/integrations/toss-payments/test-orders/{orderId}/cancel:
    post:
      tags: [Integrations]
      summary: Fully cancel and ingest a confirmed Toss Payments test order
      operationId: cancelTossTestOrder
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelTossTestOrderRequest'
      responses:
        '200':
          description: Canceled payment ingestion result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TossTestOrderResult'
        '409':
          description: Order has already been processed or does not match
        '503':
          description: Toss Payments cancellation failed

components:
  parameters:
    EventId:
      name: eventId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    Month:
      name: month
      in: path
      required: true
      schema:
        type: string
        pattern: '^\d{4}-\d{2}$'
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: Resource not found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:

    CreateTossTestOrderRequest:
      type: object
      required: [amount]
      properties:
        amount:
          type: integer
          format: int64
          minimum: 100
          maximum: 1000000
        orderName:
          type: string
          maxLength: 100
    TossTestOrder:
      type: object
      required: [orderId, clientKey, amount, orderName]
      properties:
        orderId:
          type: string
        clientKey:
          type: string
          description: Public Toss Payments client key
        amount:
          type: integer
          format: int64
        orderName:
          type: string
    ConfirmTossTestOrderRequest:
      type: object
      required: [paymentKey, amount]
      properties:
        paymentKey:
          type: string
          maxLength: 200
        amount:
          type: integer
          format: int64
          minimum: 100
          maximum: 1000000
    CancelTossTestOrderRequest:
      type: object
      required: [paymentKey]
      properties:
        paymentKey:
          type: string
          maxLength: 200
    TossTestOrderResult:
      type: object
      required: [paymentKey, orderId, status, acceptedCount, duplicateCount]
      properties:
        paymentKey:
          type: string
        orderId:
          type: string
        status:
          type: string
          enum: [DONE, CANCELED]
        acceptedCount:
          type: integer
          minimum: 0
        duplicateCount:
          type: integer
          minimum: 0
    TossPaymentWebhookRequest:
      type: object
      required: [eventType, data]
      properties:
        eventType:
          type: string
        data:
          type: object
          required: [paymentKey]
          properties:
            paymentKey:
              type: string
              minLength: 1
    TossPaymentWebhookResponse:
      type: object
      required: [ignored, acceptedCount, duplicateCount]
      properties:
        ignored:
          type: boolean
        acceptedCount:
          type: integer
          minimum: 0
        duplicateCount:
          type: integer
          minimum: 0
    RegisterUserRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          maxLength: 320
        password:
          type: string
          format: password
          minLength: 8
          maxLength: 72
    UserRegisteredResponse:
      type: object
      required: [userId, email, createdAt]
      properties:
        userId:
          type: string
          format: uuid
        email:
          type: string
          format: email
        createdAt:
          type: string
          format: date-time
    SubmitSpendEventRequest:
      type: object
      required: [source, message]
      properties:
        source:
          type: string
          enum: [MANUAL_TEXT, SIMULATOR]
        externalEventId:
          type: [string, 'null']
          maxLength: 200
        message:
          type: string
          minLength: 1
          maxLength: 2000
        occurredAt:
          type: [string, 'null']
          format: date-time
    SpendEventAcceptedResponse:
      type: object
      required: [eventId, status, receivedAt]
      properties:
        eventId:
          type: string
          format: uuid
        status:
          type: string
          const: RECEIVED
        receivedAt:
          type: string
          format: date-time
    SpendEventDetail:
      type: object
      required: [eventId, source, status, receivedAt]
      properties:
        eventId:
          type: string
          format: uuid
        source:
          type: string
          enum: [MANUAL_TEXT, SIMULATOR, TOSS_WEBHOOK]
        status:
          type: string
          enum: [RECEIVED, ANALYZING, NEEDS_REVIEW]
        occurredAt:
          type: [string, 'null']
          format: date-time
        receivedAt:
          type: string
          format: date-time
        fastParse:
          oneOf:
            - $ref: '#/components/schemas/FastParseResult'
            - type: 'null'
    FastParseResult:
      type: object
      required: [currency, status, parserVersion, parsedAt]
      properties:
        amount:
          type: [number, 'null']
          minimum: 0
        currency:
          type: string
          const: KRW
        transactionType:
          type: [string, 'null']
          enum: [PAYMENT, CANCEL, REFUND, null]
        status:
          type: string
          enum: [PARSED, NEEDS_REVIEW]
        reviewReason:
          type: [string, 'null']
        parserVersion:
          type: string
        parsedAt:
          type: string
          format: date-time
    Transaction:
      type: object
      required: [id, amount, currency, transactionType, category, confidence, occurredAt]
      properties:
        id:
          type: string
          format: uuid
        amount:
          type: number
          minimum: 0
        currency:
          type: string
          default: KRW
        transactionType:
          type: string
          enum: [PAYMENT, CANCEL, REFUND]
        merchant:
          type: [string, 'null']
        normalizedMerchant:
          type: [string, 'null']
        category:
          $ref: '#/components/schemas/Category'
        confidence:
          type: number
          minimum: 0
          maximum: 1
        occurredAt:
          type: string
          format: date-time
    Category:
      type: string
      enum: [FOOD, TRANSPORTATION, SHOPPING, SUBSCRIPTION, HOUSING, HEALTH, EDUCATION, LEISURE, TRANSFER, OTHER]
    BudgetInput:
      type: object
      required: [category, budgetAmount]
      properties:
        category:
          $ref: '#/components/schemas/Category'
        budgetAmount:
          type: number
          minimum: 0
    Budget:
      allOf:
        - $ref: '#/components/schemas/BudgetInput'
        - type: object
          required: [spentAmount, utilizationRate]
          properties:
            spentAmount:
              type: number
            utilizationRate:
              type: number
    RiskAssessment:
      type: object
      required: [score, level, policyVersion, signals]
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        level:
          type: string
          enum: [LOW, MEDIUM, HIGH]
        policyVersion:
          type: string
        signals:
          type: array
          items:
            type: object
            required: [type, message]
            properties:
              type:
                type: string
              message:
                type: string
    Dashboard:
      type: object
      required: [month, totalBudget, totalSpent, budgets, recentTransactions, alerts]
      properties:
        month:
          type: string
        totalBudget:
          type: number
        totalSpent:
          type: number
        budgets:
          type: array
          items:
            $ref: '#/components/schemas/Budget'
        recentTransactions:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        alerts:
          type: array
          items:
            $ref: '#/components/schemas/RiskAssessment'
    Problem:
      type: object
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        traceId:
          type: string
